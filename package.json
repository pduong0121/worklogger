{
  "name": "work-status-logger",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"cd backend && npm start\" \"cd frontend && npm start\"",
    "install-all": "cd backend && npm install && cd ../frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^7.0.0"
  }
}
